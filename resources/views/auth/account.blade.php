@extends('layouts.store') @section('title','My Account') @section('content')
<section class="section"><div class="page-head"><div><small>MY ACCOUNT</small><h1>Hello, {{ auth()->user()->name }}</h1></div><form method="post" action="{{ route('logout') }}">@csrf<button class="link">Logout</button></form></div><h2>Orders</h2><div class="panel table-wrap"><table><tr><th>Order</th><th>Date</th><th>Status</th><th>Total</th></tr>@forelse($orders as $order)<tr><td>{{ $order->number }}</td><td>{{ $order->created_at->format('d M Y') }}</td><td><span class="badge">{{ $order->status }}</span></td><td>PKR {{ number_format($order->total) }}</td></tr>@empty<tr><td colspan="4">No orders yet.</td></tr>@endforelse</table></div><h2>Wishlist</h2><div class="product-grid">@foreach($wishlist as $product) @include('store.partials.product-card') @endforeach</div></section>
@endsection
