@extends('layouts.admin') @section('title','Home') @section('heading','Home') @section('content')
<div class="stats"><div class="card"><small>Total revenue</small><h2>PKR {{ number_format($revenue) }}</h2><span>All non-cancelled orders</span></div><div class="card"><small>Orders</small><h2>{{ number_format($orderCount) }}</h2><span>Lifetime</span></div><div class="card"><small>Products</small><h2>{{ number_format($productCount) }}</h2><span>Catalog items</span></div><div class="card"><small>Customers</small><h2>{{ number_format($customerCount) }}</h2><span>Registered accounts</span></div></div>
<div class="card"><div class="card-head"><div><h2>Recent orders</h2><p>The latest activity from your store</p></div><a class="button secondary" href="{{ route('admin.orders') }}">View all</a></div><div class="table-wrap"><table><tr><th>Order</th><th>Customer</th><th>Status</th><th>Fulfillment</th><th>Total</th><th>Date</th></tr>@foreach($orders as $order)<tr><td><b>{{ $order->number }}</b></td><td>{{ $order->customer_name }}<small>{{ $order->customer_email }}</small></td><td><span class="badge">{{ $order->status }}</span></td><td><span class="badge gray">{{ $order->fulfillment_status }}</span></td><td>PKR {{ number_format($order->total) }}</td><td>{{ $order->created_at->format('d M, H:i') }}</td></tr>@endforeach</table></div></div>
@endsection
