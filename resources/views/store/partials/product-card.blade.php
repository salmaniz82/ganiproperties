<article class="product-card"><a href="{{ route('products.show',$product) }}"><img src="{{ asset($product->thumbnail_image) }}" alt="{{ $product->name }}" loading="lazy"><h3>{{ $product->name }}</h3></a><div><b>{{ $product->formatted_price }}</b><form class="js-ajax-add-cart" method="post" action="{{ route('cart.add',$product) }}">@csrf<input type="hidden" name="quantity" value="1"><button aria-label="Add {{ $product->name }} to cart">+</button></form></div></article>
